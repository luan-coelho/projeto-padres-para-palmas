---
import { CONTACT_INFO } from '../consts'

interface Props {
  sectionLabel?: string
  title?: string
  titleHighlight?: string
}

const {
  sectionLabel = 'Entre em contato',
  title = 'Onde',
  titleHighlight = 'Estamos'
} = Astro.props
---

<section id="contato" class="relative bg-white py-20 lg:py-28">
  <div class="mx-auto max-w-4xl px-4 text-center sm:px-6 lg:px-8">
    <span class="mb-4 inline-block text-sm font-semibold tracking-wider text-azul uppercase">
      {sectionLabel}
    </span>
    <h2 class="mb-12 text-3xl font-light text-grafite sm:text-4xl lg:text-5xl">
      {title}
      <span class="font-semibold">{titleHighlight}</span>
    </h2>

    <div class="grid gap-8 md:grid-cols-2">
      <!-- Endereço -->
      <div class="rounded-2xl bg-white p-8 shadow-sm">
        <div
          class="mx-auto mb-6 flex h-16 w-16 items-center justify-center rounded-full bg-verde/10">
          <svg class="h-8 w-8 text-verde" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="1.5"
              d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z">
            </path>
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="1.5"
              d="M15 11a3 3 0 11-6 0 3 3 0 016 0z">
            </path>
          </svg>
        </div>
        <h3 class="mb-3 text-xl font-semibold text-grafite">Endereço</h3>
        <p class="text-grafite/70">{CONTACT_INFO.address}<br />{CONTACT_INFO.city}</p>
      </div>

      <!-- E-mail -->
      <div class="rounded-2xl bg-white p-8 shadow-sm">
        <div
          class="mx-auto mb-6 flex h-16 w-16 items-center justify-center rounded-full bg-azul/10">
          <svg class="h-8 w-8 text-azul" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="1.5"
              d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z">
            </path>
          </svg>
        </div>
        <h3 class="mb-3 text-xl font-semibold text-grafite">E-mail</h3>
        <a href={`mailto:${CONTACT_INFO.email}`} class="text-azul hover:underline">
          {CONTACT_INFO.email}
        </a>
      </div>
    </div>
  </div>
</section>
